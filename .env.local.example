# Copy this file to .env.local and fill in the values from the Supabase
# dashboard (Project Settings > API). Without these, locatie/, workshops/
# and admin/ load as a blank page (only the static homepage still works).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
